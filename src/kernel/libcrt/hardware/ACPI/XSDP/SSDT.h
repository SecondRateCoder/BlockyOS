#pragma once

//  The ACPI SSDT (Secondary System Descriptor Table) It contains additional AML to interact with devices. 
#define XSDT_SSDTCode    "SSDT"

#pragma message "SSDT will be properly handled further into the Boot Process, we dont need/want it right now."