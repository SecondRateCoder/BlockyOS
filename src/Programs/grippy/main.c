#include "./src/Public/Publics.h"

int main(char *command, size_t len){
    size_t cc =0;
    char *str;
    while(cc < numofpaths){
        if(strcompare_l(paths[cc].Name, ) == true){}
    }
}