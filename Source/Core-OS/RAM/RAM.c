#include "RAM.h"

#pragma region Alloca
//Thread-wise protection doesnt matter as long as the Program is enclosed.
uint8_t *Alloca(size_t Size, pID_t ProgramID){

}
#pragma endregion

uint8_t **header_encodefull(header_t h){

}

void Point_PointerFree(size_t MinimumConcurrentSize){
    size_t cc =0;
    while(Pointer < ramlength){
        // Scan through HeaderMetadata,
		// 
        cc += 2;
        ++Pointer;
    }
}