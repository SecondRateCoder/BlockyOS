#include "RAM.h"

#pragma region Alloca
uint8_t *Alloca(size_t Size){

}
#pragma endregion

void Point_PointerFree(size_t MinimumConcurrentSize){
    size_t cc =0;
    while(Pointer < ramlength){
        //Scan through HeaderMetadata, using the Address, Size tuple to find the used space.
        cc += 2;
        ++Pointer;
    }
}